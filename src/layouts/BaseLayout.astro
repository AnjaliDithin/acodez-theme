---
import "../styles/global.css";   
import "../styles/main.scss";    
import Header from "../components/Header.astro";
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@100;200;300;400;500;600;700;800;900&display=swap"
      rel="stylesheet"
    />
  </head>

  <body>
    <Header />

    <main>
      <slot />
    </main>
  </body>

  <!-- <style is:global>
   .mf-cursor {
      width: 40px;
      height: 40px;
      border: 2px solid #00aeef;
      border-radius: 50%;
      position: fixed;
      top: 0;
      left: 0;
      pointer-events: none;
      z-index: 9;
      background: rgba(0, 174, 239, 0.12);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 10px;
      font-weight: 600;
      text-transform: uppercase;
      color: transparent; 
      line-height: 1;
      overflow: hidden;
      transition: width 0.2s ease,height 0.2s ease,font-size 0.2s ease,background 0.2s ease,color 0.2s ease;
  }
  .mf-cursor:before{
    opacity:0!important;
  }
  .mf-cursor.-text{
    background: #00aeef;
    color: #ffffff;
    border-color: #00aeef;
    width: 150px;
    height:150px;
  }
  .mf-cursor-text{
    font-size: 20px!important;
    font-weight: 500;
  }

  </style> -->
</html>
